- ruleID: httpcomponents-client-4-to-httpcomponents-client-5-00140
  description: EntityTemplate has been relocated from org.apache.http.entity to org.apache.hc.core5.http.io.entity in HttpClient 5.x...
  category: mandatory
  effort: 5
  labels:
    - konveyor.io/source=httpcomponents-client-4
    - konveyor.io/target=httpcomponents-client-5
    - konveyor.io/generated-by=ai-rule-gen
    - konveyor.io/test-result=passed
    - konveyor.io/review=unreviewed
  message: |-
    The `EntityTemplate` class has moved from `org.apache.http.entity` to `org.apache.hc.core5.http.io.entity` in HttpClient 5.x. In 5.x, the constructor signature has changed and `setContentType` is set via constructor parameters or builder rather than a separate setter.

    ```java
    // Before
    import org.apache.http.entity.EntityTemplate;
    EntityTemplate entity = new EntityTemplate(outstream -> { ... });
    entity.setContentType(ContentType.APPLICATION_JSON.toString());

    // After
    import org.apache.hc.core5.http.io.entity.EntityTemplate;
    EntityTemplate entity = new EntityTemplate(-1, ContentType.APPLICATION_JSON, null, outstream -> { ... });
    ```
  when:
    java.referenced:
        pattern: org.apache.http.entity.EntityTemplate
        location: IMPORT
- ruleID: httpcomponents-client-4-to-httpcomponents-client-5-00150
  description: ContentType has been relocated from org.apache.http.entity to org.apache.hc.core5.http in HttpClient 5.x.
  category: mandatory
  effort: 1
  labels:
    - konveyor.io/source=httpcomponents-client-4
    - konveyor.io/target=httpcomponents-client-5
    - konveyor.io/generated-by=ai-rule-gen
    - konveyor.io/test-result=passed
    - konveyor.io/review=unreviewed
  message: |-
    The `ContentType` class has moved from `org.apache.http.entity` to `org.apache.hc.core5.http` in HttpClient 5.x. Update your import accordingly.

    ```java
    // Before
    import org.apache.http.entity.ContentType;

    // After
    import org.apache.hc.core5.http.ContentType;
    ```
  when:
    java.referenced:
        pattern: org.apache.http.entity.ContentType
        location: IMPORT
- ruleID: httpcomponents-client-4-to-httpcomponents-client-5-00160
  description: HttpEntity interface has been relocated from org.apache.http to org.apache.hc.core5.http in HttpClient 5.x.
  category: mandatory
  effort: 1
  labels:
    - konveyor.io/source=httpcomponents-client-4
    - konveyor.io/target=httpcomponents-client-5
    - konveyor.io/generated-by=ai-rule-gen
    - konveyor.io/test-result=passed
    - konveyor.io/review=unreviewed
  message: |-
    The `HttpEntity` interface has moved from `org.apache.http` to `org.apache.hc.core5.http` in HttpClient 5.x. Update your import accordingly.

    ```java
    // Before
    import org.apache.http.HttpEntity;

    // After
    import org.apache.hc.core5.http.HttpEntity;
    ```
  when:
    java.referenced:
        pattern: org.apache.http.HttpEntity
        location: IMPORT
